# project-brief: layered-studios

## purpose
Local-first content production tooling and pipelines for series/episode assets.

## current state
In progress (scripts and local tooling work). Needs stability + ops integration.

## definitions
- done means: studio tools produce deterministic artifacts and log sessions automatically.

## stack / tools
PowerShell pipelines + local-first structure.

## repos / paths
- folder: projects/layered-studios

## non-negotiables
- deterministic ingest/build
- ops logging to lv_ops

## top priorities
1) stabilize tooling + health script issues
2) unify ingest/build logs into lv_ops
3) standardize project briefs for series work
